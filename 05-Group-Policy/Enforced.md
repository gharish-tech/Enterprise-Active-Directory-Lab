# Enforced GPO

## Purpose

Force a Group Policy to apply even if child OUs have Block Inheritance enabled.

## Example

Domain Password Policy = Enforced

HR OU = Block Inheritance Enabled

Result

The Password Policy still applies to HR.