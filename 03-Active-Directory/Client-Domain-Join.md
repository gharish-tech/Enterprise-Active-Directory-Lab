## Internal Working

When a domain user logs in, the client computer sends an authentication request to the Domain Controller. The Domain Controller verifies the user's credentials against the Active Directory database and returns the authentication result. If successful, the user is granted access according to their group memberships and permissions.
